To build this example, install cmake and run

```
cmake -B build
cmake --build build
```

Executable `example` will appear in `build` directory, you can run this executable without any command line arguments

